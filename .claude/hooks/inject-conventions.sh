#!/bin/bash
# UserPromptSubmit hook: remind the agent of project conventions every turn.
echo "Reminder: no \`any\`; fetch via src/hooks/; pure logic in src/domain/ with tests; thresholds only in src/config.ts; keep components <80 lines, complexity <10."
exit 0
